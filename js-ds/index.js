setEntries
entries = [
    {},
    {},
    {}
]

entries[1] = {}

newE 

setEntries(
    newE
)


find the entry from the array 
update the entry 
updatedEntries[index] = {}

const index = entries.forEach((entry, index) => {
    if (entry.id === e.id) {
        return index
    }
})